import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    backgroundColor: '#cecece',
    width: '99%',
    height: '5%',
    borderRadius: 15,
    alignItems: 'center',
  },
});
